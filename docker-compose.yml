version: '3.7'

services: 

    website:
        container_name: rs_website
        build:
            context: .
            dockerfile: Dockerfile
        volumes: 
            - .:/app
            - /app/node_modules
        ports:
            - 4201:4200